import ChatMessage from "@/components/ChatMessage";

export const metadata = {
    title: "Chat",
}

const ChatPage = () => {
    return (
        <div>
            <ChatMessage />
        </div>
    )
};

export default ChatPage;